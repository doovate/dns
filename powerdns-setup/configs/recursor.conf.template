# PowerDNS Recursor configuration (template)
# Rendered by install.sh using values from config.env

setuid=pdns
setgid=pdns

# Listen on all interfaces for client queries
local-address=0.0.0.0
local-port={{PDNS_RECURSOR_PORT}}

# Forward unresolved queries to public resolvers
forward-zones-recurse=.= {{DNS_FORWARDER_1}};{{DNS_FORWARDER_2}}

# Forward internal zone to local authoritative
forward-zones={{DNS_ZONE}}=127.0.0.1:{{PDNS_AUTH_PORT}}

# Logging
log-common-errors=yes
quiet=no
